<li>
  <%= link_to player.name, player %>
  <% if logged_in? && current_player.admin? && !current_player?(player) %>
  | <%= link_to "deactivate", player_activation_path(:id => player.id, :isActive => false), method: :patch,
                                                       data: { confirm: "You sure?" } %>
  <% end %>
</li>
